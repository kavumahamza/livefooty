# Task 4.1 Report — Vite + React app, polling hook, dev proxy

## Status: COMPLETE ✓

---

## Files Created / Modified

| Path | Action |
|------|--------|
| `frontend/` | Scaffolded via `npm create vite@latest . -- --template react` (plain JS, NOT TypeScript) |
| `frontend/vite.config.js` | Modified — added `/api` proxy + vitest config |
| `frontend/src/api/poll.js` | Created — `usePoll` hook |
| `frontend/src/api/rest.js` | Created — `fetchLive`, `fetchFixtures`, `fetchMatch` helpers |
| `frontend/src/api/poll.test.jsx` | Created — 4-test vitest suite |
| `frontend/src/App.jsx` | Replaced scaffold with live-polling demo UI |
| `frontend/.gitignore` | Vite-generated — `node_modules` and `dist` already present |

---

## Versions Installed

- **Vite:** 8.0.16
- **React:** 19.1.0 (react + react-dom)
- **Vitest:** 4.1.9
- **@testing-library/react:** (installed as devDep, jsdom environment)

---

## `usePoll` API

```js
const { data, lastUpdatedAt, error, loading } = usePoll(url, intervalMs)
```

- `data` — parsed JSON from last successful fetch (null until first success)
- `lastUpdatedAt` — `Date.now()` timestamp of last successful fetch
- `error` — Error object if last fetch failed, else null
- `loading` — true until first response (success or failure)

**Backoff behaviour:** On any fetch failure, the hook sets `skipNextRef.current = true`. The next interval tick detects this and skips the fetch (resetting the flag), effectively doubling the wait after an error before retrying. This is intentionally minimal — one skipped tick, no exponential ramp.

**Cleanup:** Each effect registers an `AbortController`. On unmount (or url/intervalMs change), cleanup: `clearInterval`, `controller.abort()`, and `mountedRef.current = false`. Any in-flight fetch that resolves after abort is silently dropped (AbortError guard in catch).

---

## Proxy Config (`vite.config.js`)

```js
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    },
  },
},
```

Frontend calls `/api/live` same-origin in dev; Vite forwards to Django on port 8000. No CORS headers or credentials headaches in dev.

---

## Build Output (tail)

```
vite v8.0.16 building client environment for production...
✓ 18 modules transformed.
dist/index.html                   0.45 kB │ gzip:  0.29 kB
dist/assets/index-DykytF2W.css    4.10 kB │ gzip:  1.47 kB
dist/assets/index-D_frB5sP.js   191.70 kB │ gzip: 60.54 kB
✓ built in 101ms
```

---

## Vitest Output

```
 RUN  v4.1.9 /home/kavumah/Desktop/football-live/frontend

 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  18:25:10
   Duration  733ms (transform 29ms, setup 0ms, import 101ms, tests 41ms, environment 487ms)
```

Tests passing:
1. fetches once immediately on mount
2. fetches again after intervalMs elapses
3. sets error and retains prior data on a rejected fetch
4. stops fetching after unmount (interval is cleared)

---

## node_modules NOT Committed

Confirmed: `frontend/node_modules` and `frontend/dist` are both listed as ignored by git (`git check-ignore` returns them). The `.gitignore` inside `frontend/` (generated by Vite) contains both entries.

---

## Self-Review

- Hook is correct: immediate fetch on mount, interval re-subscribe on url/intervalMs change, no setState after unmount via mountedRef + AbortController guard.
- Backoff is simple by design (one-tick skip). Not exponential — per brief: "keep it SIMPLE, don't over-engineer."
- Test file is `.jsx` (not `.js`) because it contains JSX — required for correct Vite/vitest transform.
- `vi.advanceTimersByTimeAsync(ms)` used instead of `vi.runAllTimers()` (which caused infinite-loop detection from setInterval) or `vi.runAllMicrotasksAsync()` (which doesn't exist in vitest 4.x).
- `rest.js` is intentionally minimal: three functions, no state, no retry logic — that belongs to the hook layer.
- `App.jsx` is a placeholder that will be replaced by later tasks (FixturesBrowser, LiveScoreList, MatchCenter).

## Concerns

- None blocking. The test for "fetches again after intervalMs" relies on the backoff not triggering on the first tick (skipNextRef starts false) — this is correct and the test confirms it.
- The `dist/` folder from the build is present locally but gitignored — OK.
